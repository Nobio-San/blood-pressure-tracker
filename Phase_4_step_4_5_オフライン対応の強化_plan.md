## 前提・要約
- 目的：PWA（血圧記録アプリ）をオフラインでも「入力・閲覧・保存」できる状態にし、オンライン復帰時にGoogle Sheetsへ自動同期してデータ欠損を防ぐ。
- 成果物：
  - 改善された Service Worker（キャッシュ戦略の明確化／オフライン時の挙動安定化）
  - オフライン状態のUI（バナー・同期待ち件数・同期ステータス）
  - 同期キュー（オフライン保存→オンライン復帰時に自動送信）
  - （オプション）Background Sync 対応（対応端末ではバックグラウンド同期）
- 対象の種類（推定）：改修（PWA/Service Worker強化 + 同期ロジック追加）。※既存PWA/Sheets連携を前提に、信頼性を上げる改善。
- 成功条件（Done）：
  - オフライン時に、記録の追加が失敗せずローカルに保存される
  - オフライン時に、既存記録の一覧・グラフ表示ができる（少なくとも直近データ）
  - オンライン復帰時に、未送信データが自動でGoogle Sheetsへ送信され、送信済みとしてキューから消える
  - UI上でオンライン/オフライン状態と「同期待ち件数」が確認できる
  - 主要ブラウザ（Android Chrome中心）で致命的な劣化がない
- 前提（仮定した点があれば明記）：
  - 既にPWA基本（manifest + service-worker登録）が動作している
  - データ保存は localStorage を中心に行い、Sheets送信は Apps Script WebApp へ fetch でPOSTしている
  - サーバ側（Apps Script）の大幅改修は避け、クライアント側での再送・重複対策を優先する（必要なら最小限の変更案もPlanに含める）
- 主要な制約：
  - ビルド工程なし（静的ファイル配信前提）
  - 端末差があるため、Background Sync は progressive enhancement（使えない場合はフォールバック）
  - Service Worker キャッシュはデバッグを難しくするため、バージョニングとクリア手順を組み込む
- 既知の不明点（あれば）：
  - 現在の service-worker.js の具体的な実装（precache対象、fetchハンドリング有無）
  - Sheets保存のAPIエンドポイント（URLパターン）とレスポンス仕様（成功判定条件）
  - 画像（撮影データ）をネットワーク取得しているか（基本はBase64/ローカル想定）

## 全体ステップ構成
- Step1: 現状把握と設計（キャッシュ対象・同期対象の棚卸し）
- Step2: Service Worker キャッシュ戦略の実装（Static/API/Images）
- Step3: オフライン検知とUI（バナー・同期待ち件数・同期状態）
- Step4: 同期キュー実装（再送・失敗時リトライ・重複対策）
- Step5: （任意）Background Sync 対応（対応環境のみ）
- Step6: E2E検証（オフライン→復帰、キャッシュ更新、整合性）

---

## Step1: 現状把握と設計（キャッシュ対象・同期対象の棚卸し）
### Plan 1: Service Worker/通信フローの現状確認と方針決定
- 目的：既存実装を壊さずに、最小変更で「オフライン体験＋同期」を成立させる設計を固める。
- 作業内容（チェックリスト）：
  - [ ] 現在の service-worker.js を読み、precache対象（HTML/CSS/JS/アイコン/CDN）と fetch ハンドリング有無を確認
  - [ ] Sheets送信（POST）の呼び出し箇所と、失敗時の挙動（例：catchでローカルのみ保存）を確認
  - [ ] 「同期すべきデータ」の定義（= Sheets未送信の記録）と保存先（localStorage内のキュー or IndexedDB）を決める
  - [ ] 重複送信の回避方針を決める（クライアント側 idempotency / サーバ側 dedupe のどちらを採るか）
  - [ ] キャッシュバージョン運用（CACHE_VERSION など）と更新時の破壊的影響（古いキャッシュ残り）を整理
- 変更対象（想定ファイル/モジュール）：
  - service-worker.js
  - js/app.js（Sheets送信呼び出し周り）
  - （必要なら）js/sheets-api.js（エラー判定/リトライしやすいIFに）
- 受け入れ基準（完了条件）：
  - キャッシュ対象・ネットワーク対象・同期対象が文章で整理され、次Planの実装観点がブレない
- テスト/検証：
  - DevTools（Application > Service Workers / Cache Storage）で現状キャッシュを確認
- 依存関係・注意点：
  - 以降のPlanは、この棚卸し結果に依存
- リスクと回避策：
  - リスク：SWの更新で開発中に古いキャッシュが残り混乱
  - 回避：キャッシュバージョンを明確にし、更新時に古いキャッシュを削除する

---

## Step2: Service Worker キャッシュ戦略の実装（Static/API/Images）
### Plan 1: Static assets を Cache First で安定化（Precache + バージョニング）
- 目的：オフラインでもアプリシェル（UI）が確実に起動する状態を作る。
- 作業内容（チェックリスト）：
  - [ ] install で HTML/CSS/JS/manifest/icons など必要最小限を precache
  - [ ] activate で旧キャッシュを削除（CACHE_VERSION を導入）
  - [ ] navigation（index.html）へのアクセスは、オフライン時にキャッシュへ確実にフォールバック
  - [ ] CDN依存（Chart.js/Tesseract.js等）がある場合の方針を決める：
    - 可能ならローカル同梱 or キャッシュ対象に含める
    - 難しい場合、オフライン時は「一部機能制限」表示（例：OCRはネット必要）
- 変更対象（想定ファイル/モジュール）：
  - service-worker.js
  - index.html（CDN依存の棚卸しに応じて）
- 受け入れ基準（完了条件）：
  - DevToolsで Offline にしても、アプリが起動し主要画面が表示できる
- テスト/検証：
  - DevTools > Network: Offline でリロードして表示確認
  - Application > Cache Storage に想定キャッシュが作成されている
- 依存関係・注意点：
  - precache対象のURLは配置パスに依存（start_url や相対パスに注意）
- リスクと回避策：
  - リスク：キャッシュが肥大化
  - 回避：アプリシェルは最小限に限定し、画像などは別戦略に分離

### Plan 2: API requests を Network First（フォールバック明確化）
- 目的：オンライン時は最新データ取得を優先しつつ、オフライン時に破綻しない。
- 作業内容（チェックリスト）：
  - [ ] Sheets取得（GET）がある場合、Network First + タイムアウト + キャッシュ/ローカルフォールバックを定義
  - [ ] Sheets送信（POST）は原則キャッシュ対象外（SWではなく app.js のキューで担保）
  - [ ] API URL 判定ルール（Apps ScriptのURLやパス）をSW内で明確にする
- 変更対象（想定ファイル/モジュール）：
  - service-worker.js
  - js/sheets-api.js（必要ならGETの扱い整理）
- 受け入れ基準（完了条件）：
  - オフライン時にAPIアクセスが発生しても、アプリが固まらず適切にフォールバックする
- テスト/検証：
  - オフライン状態でGET/POSTが走ったときのコンソールエラーが制御され、UIが継続利用できる
- 依存関係・注意点：
  - Apps ScriptはCORS/キャッシュ制御が弱い場合があるため、必要以上にSWでいじらない
- リスクと回避策：
  - リスク：Network First の待ち時間でUXが悪化
  - 回避：短いタイムアウトとローカル即時表示（後から更新）に寄せる

### Plan 3: Images を Cache First（将来の拡張に備える）
- 目的：アイコンや外部画像があってもオフラインで破綻しない。
- 作業内容（チェックリスト）：
  - [ ] request.destination === 'image' などで画像リクエストを判定し Cache First
  - [ ] キャッシュ上限（件数/容量）の簡易ルールを決める（増えすぎる場合は古いものから削除）
- 変更対象（想定ファイル/モジュール）：
  - service-worker.js
- 受け入れ基準（完了条件）：
  - オフラインでもアイコン等が表示され、画像リクエストでエラーが連鎖しない
- テスト/検証：
  - Onlineで一度表示→Offlineで再表示して画像が表示される
- 依存関係・注意点：
  - 撮影画像がBase64で保持されている場合、SWキャッシュ対象ではない（別管理）
- リスクと回避策：
  - リスク：キャッシュ肥大化
  - 回避：画像キャッシュは別キャッシュ名に分離し、削除ポリシーを用意

---

## Step3: オフライン検知とUI（バナー・同期待ち件数・同期状態）
### Plan 1: オンライン/オフライン状態の可視化（バナー＋アイコン）
- 目的：利用者が「今オフライン」「同期されていない可能性」を直感的に把握できる。
- 作業内容（チェックリスト）：
  - [ ] navigator.onLine と online/offline イベントで状態を監視
  - [ ] 画面上部（固定）にオフラインバナーを表示（オンラインで自動非表示）
  - [ ] （任意）ヘッダーに小さなステータスアイコン（緑=online / 灰=offline）
- 変更対象（想定ファイル/モジュール）：
  - index.html（バナーDOM）
  - css/style.css（バナー/アイコンのスタイル）
  - js/app.js（状態監視・UI更新）
- 受け入れ基準（完了条件）：
  - 機内モードON/OFFでUIが即時に切り替わる
- テスト/検証：
  - Android Chromeで機内モード切替・Wi-Fi遮断で確認
- 依存関係・注意点：
  - navigator.onLine は「インターネット到達性」を保証しない（Apps Script到達は別問題）
- リスクと回避策：
  - リスク：オンライン表示なのに実際はAPIに到達できない
  - 回避：同期処理の成功/失敗を別途UIで表示（次Plan）

### Plan 2: 同期待ち件数と同期ステータス表示
- 目的：未送信データの存在と同期進行をユーザーに示し、不安を減らす。
- 作業内容（チェックリスト）：
  - [ ] 同期キュー件数を取得して表示（例：「同期待ち: 3件」）
  - [ ] 同期中はスピナー or 「同期中…」表示
  - [ ] 同期成功/失敗のトースト（簡易）を用意
- 変更対象（想定ファイル/モジュール）：
  - index.html / css/style.css
  - js/app.js
- 受け入れ基準（完了条件）：
  - キューが増減したタイミングで件数表示が追従する
- テスト/検証：
  - オフラインで記録→件数増
  - オンライン復帰→同期→件数0
- 依存関係・注意点：
  - 件数表示は、キュー実装（Step4）と密結合になるためIF（関数）を先に決める
- リスクと回避策：
  - リスク：UI更新が散らばりバグりやすい
  - 回避：UI更新関数を1か所に集約

---

## Step4: 同期キュー実装（再送・失敗時リトライ・重複対策）
### Plan 1: “送信済み/未送信” を管理する同期キュー（localStorage版）
- 目的：オフラインや一時的なネット障害でも、記録を失わずに後で必ず送る。
- 作業内容（チェックリスト）：
  - [ ] キュー保存形式を決める（例：pendingQueue: [{id, payload, createdAt, retryCount, lastTriedAt}…]）
  - [ ] 保存処理：
    - オフライン時 or Sheets送信失敗時にキューへ追加
    - 既に同じ id がキューにある場合は重複追加しない
  - [ ] 再送処理（flush）：
    - onlineイベントで自動起動
    - 逐次送信（1件ずつ）し、成功したものは削除
    - 失敗時は retryCount++、指数バックオフ相当（最低限の待ち）を検討
  - [ ] “同期中” の排他（多重起動防止フラグ）
  - [ ] 送信成功判定（HTTPステータス or レスポンスJSONの status）を統一
- 変更対象（想定ファイル/モジュール）：
  - js/app.js（保存フロー/オンライン監視/flush）
  - js/sheets-api.js（成功判定・例外化の整理）
- 受け入れ基準（完了条件）：
  - オフラインで5件保存してもエラーで落ちず、オンライン復帰で自動送信される
  - キューの多重送信が起きない（同一idが複数回追加されない）
- テスト/検証：
  - DevTools Offlineで複数件保存→localStorageでキュー増加確認
  - Online復帰→NetworkでPOSTが順次発生→Sheetsに反映→キュー0
- 依存関係・注意点：
  - localStorageは容量制限があるため、キューは「未送信データのみ」かつ小さく保つ
- リスクと回避策：
  - リスク：送信途中でタブが閉じられて一部未送信のまま
  - 回避：キューが残る設計にし、次回起動時にもflushを走らせる

### Plan 2: 重複送信・整合性の対策（最小コスト案）
- 目的：ネットワークの再送や二重実行で、Sheetsに同じレコードが重複して増える問題を抑える。
- 作業内容（チェックリスト）：
  - [ ] クライアント側：id を必ず不変にし、キュー追加時/送信時に重複排除
  - [ ] （推奨）Apps Script側に最小の冪等性を追加する案を検討：
    - 受信した id が既に存在する場合は append しない（または上書き）
  - [ ] どこまでやるかを決め、実装範囲を固定
- 変更対象（想定ファイル/モジュール）：
  - js/app.js / js/sheets-api.js
  - （任意）Apps Script（doPost）
- 受け入れ基準（完了条件）：
  - 同じ記録を再送しても、Sheets側に重複が増えにくい（少なくともクライアント側で抑止）
- テスト/検検証：
  - 意図的にPOSTを2回投げるケースで、重複が発生する/しないを確認
- 依存関係・注意点：
  - Apps Scriptの検索は行数が増えると遅くなるため、上書き実装は要注意
- リスクと回避策：
  - リスク：Apps Script変更がスコープ外で遅延
  - 回避：まずクライアント側重複排除で最低限を担保し、必要なら後追いでサーバ改善

---

## Step5: （任意）Background Sync 対応（対応環境のみ）
### Plan 1: Background Sync の progressive enhancement
- 目的：対応ブラウザでは、アプリを閉じても同期が進む可能性を高める。
- 作業内容（チェックリスト）：
  - [ ] SyncManager 対応可否を判定し、可能なら sync を登録
  - [ ] service-worker 側で sync イベントをハンドリングし、キューflushを実行
  - [ ] 非対応環境では onlineイベント/起動時flush にフォールバック
- 変更対象（想定ファイル/モジュール）：
  - service-worker.js
  - js/app.js
- 受け入れ基準（完了条件）：
  - 対応環境で、オンライン復帰後に自動的にflushが走る（少なくとも同等の挙動）
- テスト/検証：
  - Chrome（Android）で対応可否を確認し、対応時のみ動作確認（非対応でも壊れないこと）
- 依存関係・注意点：
  - 端末/OS/省電力設定で挙動が左右されるため、必須機能として依存しない
- リスクと回避策：
  - リスク：期待したほどバックグラウンド実行されない
  - 回避：フォールバックを主ルートにし、Background Syncは“効けばラッキー”にする

---

## Step6: E2E検証（オフライン→復帰、キャッシュ更新、整合性）
### Plan 1: オフライン利用シナリオの総合テスト（実機中心）
- 目的：実利用で詰まりやすい境界（オフライン/復帰/キャッシュ更新）を潰す。
- 作業内容（チェックリスト）：
  - [ ] 初回オンラインでアクセス→SWインストール→再起動（キャッシュが効くこと）
  - [ ] オフラインで：起動→一覧/グラフ表示→新規記録→キュー増加
  - [ ] オンライン復帰で：自動同期→Sheets反映→キュー0→UIがオンライン表示
  - [ ] APIが一時的に失敗するケース（オンラインだがPOST失敗）でキューが溜まり、後で送れる
  - [ ] SW更新（CACHE_VERSION更新）時に古いキャッシュが消え、新しい版が正常に起動する
- 変更対象（想定ファイル/モジュール）：
  - 変更なし（検証結果に応じて service-worker.js / js/app.js を微調整）
- 受け入れ基準（完了条件）：
  - 上記シナリオが再現性をもって成功し、致命的なデータ欠損や二重送信が起きない
- テスト/検証：
  - Android Chrome（PWAインストール状態）で実施
  - DevToolsのOffline/Throttlingも併用
- 依存関係・注意点：
  - 実機では省電力/メモリでタブが落ちることがあるため「再起動時flush」も必須
- リスクと回避策：
  - リスク：環境差でSWが想定通り更新されない
  - 回避：アプリ内に「キャッシュを更新（再読み込み促し）」導線を用意（必要なら追加Plan化）

