## 前提・要約
- 目的：アプリ内に保存されている血圧記録データを、利用者が手元にダウンロードしてバックアップ/共有できるようにする（まずCSV、必要ならPDFレポート）。
- 成果物：
  - CSVエクスポート（全期間/期間指定・メンバー指定対応）
  - UI（エクスポート導線、期間/メンバー選択、完了/失敗フィードバック）
  - （オプション）PDFレポート（グラフ + 統計情報）
- 対象の種類（推定）：機能追加（既存データ保存/表示基盤にエクスポート機能を追加）
- 成功条件（Done）：
  - CSVがダウンロードでき、Excelで開いても文字化けしない（UTF-8 + BOM）
  - 列順が仕様通り（日時,メンバー,最高血圧,最低血圧,脈拍）で、データ件数/内容が画面表示と一致する
  - 期間指定（全期間 or 選択期間）とメンバー別エクスポートが期待通りに反映される
  - 主要ブラウザ（Android Chromeを最低限）で動作し、オフラインでもローカルデータのCSV出力ができる
- 前提（仮定した点があれば明記）：
  - 画面表示/グラフ用に「記録一覧のソースとなる配列（records）」がapp.js内で取得できる（localStorageから読み込み、必要に応じてSheets同期済み）
  - 記録データ構造は概ね { id, member, systolic, diastolic, pulse, datetime }（ISO文字列）
  - 期間指定は「datetime」の範囲でフィルタする（端末ローカル時刻基準）
- 主要な制約：
  - PWA/モバイルブラウザでのファイル保存はBlob + a[download] 方式を前提
  - Excel互換のための文字コード/改行/クォート処理が必要
- 既知の不明点（あれば）：
  - 期間選択UIをStep 4-2（グラフ強化）と共通にするか、Step 4-3専用に持つか（共通化する場合はUI状態管理の調整が必要）

## 全体ステップ構成
- Step1: 要件確定・I/F整理（データ源/フィルタ条件/CSV仕様の固定）
- Step2: CSV変換・ダウンロード基盤の実装（エンコード/クォート/ファイル名）
- Step3: UI実装（期間/メンバー選択、ボタン、フィードバック）
- Step4: （オプション）PDFレポート出力（グラフ + 統計情報）
- Step5: テスト/互換性確認・ドキュメント整備

---

## Step1: 要件確定・I/F整理
### Plan 1: エクスポート対象データとフィルタ仕様の確定
- 目的：CSV/PDFに出すデータの「元」と「絞り込み条件」を固定し、実装の迷いをなくす。
- 作業内容（チェックリスト）：
  - [ ] app.js内のデータ取得経路を確認（localStorage / Google Sheets / 両方のマージ）
  - [ ] エクスポート対象を「画面表示の基準データ（records）」に統一する方針を決める
  - [ ] フィルタの仕様を固定
    - [ ] 期間：全期間 / 開始日〜終了日（日時の包含ルール：開始は00:00、終了は23:59:59相当など）
    - [ ] メンバー：全員 / 1人（既存のメンバー選択と共通化）
  - [ ] CSV列仕様を固定（ヘッダ有無、列順、日時フォーマット）
- 変更対象（想定ファイル/モジュール）：
  - js/app.js（既存のrecords管理・フィルタ関数を参照）
  - index.html（UI追加前の配置検討）
- 受け入れ基準（完了条件）：
  - フィルタ・列仕様をREADMEまたはコメントで明文化できている
- テスト/検証：
  - 既存の記録一覧と同じ並び/件数になる条件を手計算で1ケース確認
- 依存関係・注意点：
  - Step 4-2に期間選択UIが入る場合、UI状態を共有する設計にすると手戻りが減る
- リスクと回避策：
  - リスク：データ源が画面表示とエクスポートでズレる
  - 回避策：「画面で使っているrecords」をエクスポートも参照するよう統一

### Plan 2: CSVの互換性仕様（Excel）を固定
- 目的：ダウンロード後にExcelで開いた際の文字化け/改行崩れを防ぐ。
- 作業内容（チェックリスト）：
  - [ ] UTF-8 + BOM を採用
  - [ ] 改行コードは \r\n を採用
  - [ ] セル値のクォートルールを決める（カンマ/改行/ダブルクォートを含む場合は"で囲み、内部"は""にエスケープ）
  - [ ] 日時の出力形式を決める（例：YYYY/MM/DD HH:mm）
- 変更対象（想定ファイル/モジュール）：
  - js/app.js
- 受け入れ基準（完了条件）：
  - 仕様（エンコード・改行・クォート）が実装方針として固定できている
- テスト/検証：
  - テスト用データに「カンマ・改行・ダブルクォート」を含むメンバー名/メモ（将来拡張想定）を入れた場合もCSVが壊れない前提になる
- 依存関係・注意点：
  - 現状フィールドが数値中心でも、将来的な拡張（メモ欄等）に備えてクォート処理は必須
- リスクと回避策：
  - リスク：OS/Excel設定によっては日付解釈が崩れる
  - 回避策：日時を文字列として出す（スラッシュ区切り、ゼロ埋め）

---

## Step2: CSV変換・ダウンロード基盤の実装
### Plan 1: フィルタ済みデータ取得関数の追加
- 目的：UIから渡された条件（期間/メンバー）に基づき、エクスポート対象レコード配列を返す。
- 作業内容（チェックリスト）：
  - [ ] `getExportRecords({ member, range })` のような関数をapp.jsに追加（または既存フィルタ関数を再利用）
  - [ ] 期間の境界条件を実装（開始/終了の包含、未指定時の扱い）
  - [ ] ソート順を決める（推奨：日時昇順で出力。画面は降順でもCSVは昇順の方が分析しやすい）
- 変更対象（想定ファイル/モジュール）：
  - js/app.js
- 受け入れ基準（完了条件）：
  - 指定条件でフィルタされたrecordsが取得できる（コンソールで件数/先頭末尾が確認できる）
- テスト/検証：
  - 期間を絞ったときに件数が減ること、メンバー指定で対象が限定されることを確認
- 依存関係・注意点：
  - datetimeがISO文字列の場合、Date変換のタイムゾーン差異に注意
- リスクと回避策：
  - リスク：ローカル時刻と保存時刻のズレ
  - 回避策：表示・グラフと同じ変換関数（フォーマット/パース）を使い回す

### Plan 2: CSV生成ユーティリティ実装
- 目的：records配列からCSV文字列（BOM付・CRLF・クォート済み）を生成する。
- 作業内容（チェックリスト）：
  - [ ] `toCsv(records)` を実装
    - [ ] ヘッダ行：`日時,メンバー,最高血圧,最低血圧,脈拍`
    - [ ] 1行生成：フォーマット済み日時 + 値
    - [ ] クォート/エスケープ関数 `escapeCsvCell(value)` を実装
    - [ ] 行結合はCRLF
    - [ ] 先頭にUTF-8 BOM（\uFEFF）を付与
  - [ ] 値の欠損時の扱い（空欄）を定義
- 変更対象（想定ファイル/モジュール）：
  - js/app.js（規模が大きければ `js/export.js` を新規作成して分離も可）
- 受け入れ基準（完了条件）：
  - 生成したCSVをテキストとして確認し、カラム数が揃っている
- テスト/検証：
  - 小さなサンプルrecords（2〜3件）で出力の目視確認
- 依存関係・注意点：
  - 数値は文字列化するが、Excel側の解釈で数値になるのは許容
- リスクと回避策：
  - リスク：CSVの改行/クォート不備でExcelが崩れる
  - 回避策：必ずescape関数を通す + CRLFを固定

### Plan 3: ブラウザダウンロード実装（Blob + download）
- 目的：生成したCSVを端末にダウンロードさせる。
- 作業内容（チェックリスト）：
  - [ ] `downloadTextFile(filename, content, mime)` を実装
  - [ ] `Blob([content], { type: 'text/csv;charset=utf-8;' })` を使用
  - [ ] `URL.createObjectURL` でURL生成し、`a.download` で保存
  - [ ] クリック後に `URL.revokeObjectURL` で解放
  - [ ] iOS/Safari等の制限を考慮（可能な範囲でフォールバック：新規タブ表示など）
- 変更対象（想定ファイル/モジュール）：
  - js/app.js
- 受け入れ基準（完了条件）：
  - Android ChromeでCSVが保存され、ファイルが開ける
- テスト/検証：
  - 実機/エミュレータでダウンロード動作確認
- 依存関係・注意点：
  - PWAのインストール状態でも保存動作が変わる可能性があるため確認する
- リスクと回避策：
  - リスク：端末/ブラウザ依存で保存UIが異なる
  - 回避策：最低限Android Chromeを必須対象にし、他は「動けばラッキー」ではなく注意書きを用意

---

## Step3: UI実装（期間/メンバー選択、ボタン、フィードバック）
### Plan 1: エクスポートUIの追加（最小）
- 目的：ユーザーが迷わずCSVを出力できる導線を提供する。
- 作業内容（チェックリスト）：
  - [ ] 「データをダウンロード」ボタンを追加
  - [ ] ボタン配置（推奨：記録一覧/グラフ付近のカード内）
  - [ ] 実行中のローディング状態（disabled + スピナー/テキスト）
  - [ ] 成功/失敗メッセージ（トースト or インライン）
- 変更対象（想定ファイル/モジュール）：
  - index.html
  - js/app.js
  - css/style.css（既存コンポーネントに合わせて微調整が必要な場合のみ）
- 受け入れ基準（完了条件）：
  - ボタン押下でCSVがダウンロードされ、メッセージが表示される
- テスト/検証：
  - データ0件時に「エクスポート対象がありません」等を出せる
- 依存関係・注意点：
  - Step 4-1（デザイン洗練）と競合する場合はクラス設計を合わせる
- リスクと回避策：
  - リスク：誤タップ連打で多重ダウンロード
  - 回避策：実行中はボタン無効化

### Plan 2: 期間指定/メンバー別エクスポートUI
- 目的：要件にある「全期間 or 選択期間」「メンバー別」をUIで提供する。
- 作業内容（チェックリスト）：
  - [ ] 期間選択を追加（以下いずれか）
    - [ ] A案：既存の期間セレクト（7/30/90/全期間）を流用（Step 4-2と統合）
    - [ ] B案：開始日・終了日のdate入力を用意（より柔軟）
  - [ ] メンバー選択を追加（既存のメンバーセレクトを流用 or エクスポート用に別セレクト）
  - [ ] 「プレビュー（対象件数表示）」を追加（任意だがUXが上がる）
- 変更対象（想定ファイル/モジュール）：
  - index.html
  - js/app.js
- 受け入れ基準（完了条件）：
  - 指定条件に応じてCSVの内容（件数/先頭末尾）が変わる
- テスト/検証：
  - 全期間/特定期間/特定メンバーでそれぞれ1ケースずつ出力確認
- 依存関係・注意点：
  - グラフの期間選択と同一状態にする場合、状態管理（グローバル変数/DOM状態）を整理する
- リスクと回避策：
  - リスク：UIが増えて煩雑
  - 回避策：折りたたみ（詳細設定）に入れる、デフォルトは「全期間・全員」にする

### Plan 3: ファイル名生成ルールの実装
- 目的：ダウンロードしたファイルが後で識別しやすいようにする。
- 作業内容（チェックリスト）：
  - [ ] 仕様通りの基本名：`blood_pressure_YYYYMMDD.csv`
  - [ ] （任意）メンバー/期間を含める拡張：`blood_pressure_{member|all}_{YYYYMMDD}_{from}-{to}.csv` など
  - [ ] ファイル名に使えない文字（/ : など）の除去
- 変更対象（想定ファイル/モジュール）：
  - js/app.js
- 受け入れ基準（完了条件）：
  - 端末保存時に想定ファイル名になっている
- テスト/検証：
  - メンバー名が日本語でもファイル名生成で壊れない（必要ならローマ字/固定のall）
- 依存関係・注意点：
  - 日本語ファイル名は環境により崩れることがあるため、既定は英数字推奨
- リスクと回避策：
  - リスク：長すぎるファイル名
  - 回避策：基本は仕様の最小名に留め、詳細は省略

---

## Step4: （オプション）PDFレポート出力（グラフ + 統計情報）
### Plan 1: PDF出力のスパイク（可否検証）
- 目的：jsPDFで「グラフ画像 + 統計情報」のPDFが端末で問題なく生成できるか検証する。
- 作業内容（チェックリスト）：
  - [ ] jsPDFをCDN導入（index.html）
  - [ ] Chart.jsのcanvasを画像化（`toDataURL`）してPDFに貼れるか確認
  - [ ] 日本語フォントの扱い方針を決める（日本語出力が必要ならフォント埋め込み検討）
- 変更対象（想定ファイル/モジュール）：
  - index.html
  - js/app.js
- 受け入れ基準（完了条件）：
  - 1ページの簡易PDF（タイトル + グラフ画像 + 平均値）が生成できる
- テスト/検証：
  - Android ChromeでPDF保存/共有ができる
- 依存関係・注意点：
  - 日本語文字を確実に出したい場合、フォント埋め込みが手間なので、初期は英数字中心のPDFにするのも選択肢
- リスクと回避策：
  - リスク：PDF生成が重く、端末で落ちる
  - 回避策：画像解像度を抑える、統計情報は最小にする

### Plan 2: PDFレポート本実装（必要なら）
- 目的：利用者が医師に共有できるレベルの簡易レポートを出せるようにする。
- 作業内容（チェックリスト）：
  - [ ] PDFメニュー/ボタン追加（CSVと並列、またはドロップダウン）
  - [ ] レポート内容
    - [ ] タイトル、期間、メンバー
    - [ ] グラフ（該当期間の表示と一致）
    - [ ] 統計（平均/最大/最小/測定回数）
    - [ ] （任意）直近N件の表
  - [ ] ファイル名：`blood_pressure_report_YYYYMMDD.pdf`
- 変更対象（想定ファイル/モジュール）：
  - index.html
  - js/app.js
- 受け入れ基準（完了条件）：
  - 指定条件に連動したPDFが生成できる
- テスト/検証：
  - 期間/メンバー変更後にPDF内容が更新される
- 依存関係・注意点：
  - 統計はStep 4-2で計算済みのロジックがあれば再利用する
- リスクと回避策：
  - リスク：日本語が豆腐になる
  - 回避策：英数字主体のレイアウト、またはフォント埋め込み対応（後回し可）

---

## Step5: テスト/互換性確認・ドキュメント整備
### Plan 1: CSV出力の受け入れテスト（Excel/Google Sheets）
- 目的：実利用で問題が起きやすい「文字化け」「列ズレ」を先に潰す。
- 作業内容（チェックリスト）：
  - [ ] Android端末でCSVをダウンロード
  - [ ] PCに転送してExcelで開く（文字化け無し、列が揃う）
  - [ ] Googleスプレッドシートにインポートしても崩れない
  - [ ] 期間/メンバー条件の違いで内容が正しいことを確認
- 変更対象（想定ファイル/モジュール）：
  - なし（修正が出た場合はapp.js）
- 受け入れ基準（完了条件）：
  - 仕様の確認ポイント（ダウンロード可/Excel可/文字化け無し）を満たす
- テスト/検証：
  - 0件、1件、複数件（>=10件）で試す
- 依存関係・注意点：
  - 端末の共有/保存ダイアログは環境差があるため、最低限の動線が確保できていればOK
- リスクと回避策：
  - リスク：Excelの地域設定で区切り文字が「;」の環境
  - 回避策：日本の一般的環境はカンマだが、必要ならTSV切替オプションを将来検討（今回はスコープ外）

### Plan 2: UI/UX・エラー処理の磨き込み
- 目的：失敗時もユーザーが次に取る行動が分かる状態にする。
- 作業内容（チェックリスト）：
  - [ ] データ0件時の文言
  - [ ] 例外発生時（Blob生成失敗等）のメッセージ
  - [ ] 実行中の二重実行防止
  - [ ] 「どの条件で出力されるか」をUI上に明記（例：『期間: 30日 / メンバー: 自分』）
- 変更対象（想定ファイル/モジュール）：
  - index.html
  - js/app.js
  - css/style.css
- 受け入れ基準（完了条件）：
  - 主要な失敗ケースでユーザーが迷わない
- テスト/検証：
  - オフライン状態でもCSV出力（ローカルデータ）が可能であることを確認
- 依存関係・注意点：
  - Step 4-5（オフライン強化）で同期キューなどが入る場合でもエクスポートはローカル基準で動くようにする
- リスクと回避策：
  - リスク：同期未完了データの扱いが曖昧
  - 回避策：「エクスポートは端末に存在するデータを出力」と明記（将来、同期状態を含めるなら別機能として扱う）

### Plan 3: README/ヘルプ追記
- 目的：家族ユーザーでもエクスポートの使い方が分かるようにする。
- 作業内容（チェックリスト）：
  - [ ] どこからダウンロードできるか
  - [ ] 期間/メンバー指定の方法
  - [ ] Excelでの開き方（文字化けしない前提だが、万一の手順も簡単に）
  - [ ] PDF（実装した場合）の用途と注意点
- 変更対象（想定ファイル/モジュール）：
  - README.md（またはヘルプUIに記載するなら index.html / app.js）
- 受け入れ基準（完了条件）：
  - エクスポート機能の使い方が1分で読める分量で記載されている
- テスト/検証：
  - 家族に見せて迷わないか簡易レビュー
- 依存関係・注意点：
  - Step 4-6（ヘルプ機能）で統合する場合はそちらに寄せる
- リスクと回避策：
  - リスク：実装とドキュメントが乖離
  - 回避策：UI文言と同じ表現を使い、変更時に同時更新するルールを作る

