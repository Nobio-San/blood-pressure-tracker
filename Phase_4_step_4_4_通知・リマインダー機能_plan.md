## 前提・要約
- 目的：血圧測定の習慣化を支援するため、通知（リマインダー/記録完了）と通知設定（ON/OFF・時刻）をPWAで提供する。
- 成果物：
  - 通知/リマインダーの設定UI（ON/OFF・朝/夜の時刻・頻度）
  - localStorageへの設定永続化
  - 記録完了時の通知
  - リマインダー通知（ベース実装＋端末/ブラウザ制約に応じたフォールバック）
  - Service Worker側の通知ハンドリング（クリック時の導線、将来のPush拡張の足場）
- 対象の種類（推定）：機能追加（Phase 4の新規機能追加）。
- 成功条件（Done）：
  - 通知設定をONにすると、ブラウザ通知の許可フローが破綻なく進み、設定が保存/復元される。
  - 「記録する」成功時に通知（または代替のトースト）が表示される。
  - リマインダーが設定時刻に（少なくともアプリ利用中は確実に）通知され、環境制約下では明確なフォールバック挙動になる。
  - Service Workerで通知クリック時にアプリが開き、入力画面へ誘導できる。
- 前提（仮定した点があれば明記）：
  - 対象プラットフォームはAndroidのChromeでインストールされたPWA（スタンドアロン）を主対象。
  - サーバーは持たず、Push配信は本Stepでは「将来拡張の足場」まで（実運用は次フェーズ）。
  - 「正確に毎日7:00/21:00にバックグラウンドで必ず出す」はWeb/PWAの制約により保証困難なため、確実性の高い範囲（アプリ稼働中・定期的に開かれる環境）で担保し、制約下はフォールバック（次回起動時の促し）にする。
- 主要な制約：
  - Web Notificationはユーザー操作を伴う許可が必要。OS/ブラウザ設定でブロックされる可能性。
  - バックグラウンドでの「ローカルスケジュール通知」はWeb標準で確実な手段が限定的（Push/Periodic系は利用可否が環境依存）。
- 既知の不明点（あれば）：
  - 想定ユーザー端末（Chromeのバージョン、PWAインストール運用の有無）
  - 望むリマインダーの厳密さ（"通知が出なかったら困る"レベルか、"だいたい出れば良い"か）

## 全体ステップ構成
- Step4-4: 通知・リマインダー機能（設計→実装→検証→将来拡張の足場）

---

## Step4-4: 通知・リマインダー機能
### Plan 1: 技術スパイク（PWA通知の実現方式決定）
- 目的：Android PWAにおける通知/リマインダーの現実的な実装方式を確定し、フォールバック方針まで含めて仕様化する。
- 作業内容（チェックリスト）：
  - [ ] 通知に関する方針整理（許可要求のタイミング、拒否時UX、再許可手順の案内）
  - [ ] リマインダー方式の選定
    - [ ] アプリ稼働中：setTimeout / setIntervalで当日次回時刻をスケジュール
    - [ ] バックグラウンド：Service Worker単体では"時間で発火"が難しいため、可能ならPeriodic系（利用可否検出）を採用、不可なら「次回起動時に通知/バナーで促し」にする
  - [ ] 設定データモデルの確定（例：notificationEnabled, morningTime, eveningTime, frequency, lastReminderSentAt など）
  - [ ] 画面/導線の確定（設定UIの配置：モーダル/設定セクション/メニュー）
  - [ ] 受け入れ基準（Plan 1内）を具体化（端末依存で保証できない部分は明文化）
- 変更対象（想定ファイル/モジュール）：
  - `js/app.js`（通知フロー呼び出し点の整理）
  - `service-worker.js`（通知クリックのハンドリング検討）
  - （追加想定）`js/notifications.js`（通知関連を分離する場合）
- 受け入れ基準（完了条件）：
  - 仕様メモ（READMEやdocs相当でも可）に「実装方式・制約・フォールバック」が明記されている。
  - localStorageのキー/スキーマが確定している。
- テスト/検証：
  - Android Chrome（通常タブ/インストールPWA）で通知許可〜表示の動作確認（手動でOK）。
- 依存関係・注意点：
  - 通知許可はユーザー操作イベント（ボタン押下等）で行う。
- リスクと回避策：
  - リスク：バックグラウンドの正確な時刻通知ができない
  - 回避策：アプリ稼働中の確実性を担保し、バックグラウンドは段階提供（まずフォールバック＋将来Push足場）

### Plan 2: 設定UI + localStorage永続化
- 目的：ユーザーが通知をON/OFFし、リマインド時刻等を設定でき、再起動後も復元されるようにする。
- 作業内容（チェックリスト）：
  - [ ] 設定UIの追加
    - [ ] 通知ON/OFFトグル
    - [ ] 朝/夜の時刻入力（例：`<input type="time">`）
    - [ ] 頻度（毎日/平日のみ/週末のみ 等）※必要なら最小は「毎日」のみ
    - [ ] 設定保存ボタン（自動保存でも可）
  - [ ] 設定のロード/保存ロジック
    - [ ] 初回デフォルト（朝07:00/夜21:00、OFF）
    - [ ] localStorageへ保存（キー例：`bp_settings`）
    - [ ] 起動時に設定反映（UIの初期値と内部状態）
  - [ ] 通知許可要求のUX
    - [ ] トグルON操作時に許可要求
    - [ ] 拒否/ブロック時は説明文（OS/ブラウザ設定から許可する手順を表示）
  - [ ] 設定変更時にスケジューリングを再計算するフックを用意
- 変更対象（想定ファイル/モジュール）：
  - `index.html`（設定セクション/モーダル追加）
  - `css/style.css`（設定UIの見た目）
  - `js/app.js`（設定の読み書き、イベント紐付け）
  - （追加想定）`js/settings.js`（分離する場合）
- 受け入れ基準（完了条件）：
  - 設定UIでON/OFF・時刻を変更できる。
  - ページ再読み込み後も設定が復元される。
  - トグルON時、許可済みなら即ON、未許可なら許可ダイアログが表示される。
  - 拒否時はONにならず、ユーザーが次に取るべき行動が画面に示される。
- テスト/検証：
  - 許可状態別（未許可/許可/ブロック）でON/OFFが破綻しないこと。
  - localStorageの中身がスキーマ通りであること。
- 依存関係・注意点：
  - 初回起動時にいきなり許可要求しない（ユーザーがONにしたときに限定）。
- リスクと回避策：
  - リスク：ユーザーが誤って拒否してしまう
  - 回避策：設定画面に「通知が出ない場合の対処」ヘルプ文を常設

### Plan 3: 記録完了通知（フォアグラウンド/Service Worker連携）
- 目的：データ保存成功時に「記録完了」を通知し、継続の動機付けを提供する。
- 作業内容（チェックリスト）：
  - [ ] 「記録する」成功時のフックに通知処理を追加
    - [ ] 通知ONかつ許可済み → Notification表示（タイトル/本文/アイコン）
    - [ ] 通知OFF or 許可なし → 既存の成功メッセージ（トースト等）だけにする
  - [ ] 通知クリック時の挙動
    - [ ] Service Workerで `notificationclick` をハンドリングし、アプリをフォーカス/起動
    - [ ] 起動時に入力画面（またはトップ）へ遷移
  - [ ] 連打抑制（短時間に複数保存した場合の通知スパム対策：同一分内は1回など）
- 変更対象（想定ファイル/モジュール）：
  - `js/app.js`（保存成功後の通知）
  - `service-worker.js`（`notificationclick`）
- 受け入れ基準（完了条件）：
  - 記録成功時に通知（許可済みの場合）が1回表示される。
  - 通知をタップするとPWAが前面に出て、トップ/入力へ誘導される。
  - 許可なし/オフの場合は通知せず、UXが壊れない。
- テスト/検証：
  - ローカル保存のみ成功（ネットワークエラー）/Sheets保存成功の両方で期待通りの挙動。
  - 通知クリックで起動・フォーカスされること。
- 依存関係・注意点：
  - Service Worker登録済み（Phase1のPWA対応）が前提。
- リスクと回避策：
  - リスク：通知が出過ぎて不快
  - 回避策：設定で記録完了通知のON/OFFを分離（必要なら）／連打抑制

### Plan 4: 測定リマインダー（スケジューラ実装 + フォールバック）
- 目的：朝/夜の測定時刻にリマインダーを出す（まずは確実に動く範囲から実装し、環境制約下はフォールバックで補う）。
- 作業内容（チェックリスト）：
  - [ ] 次回リマインダー時刻の計算ロジック
    - [ ] 朝/夜の設定時刻から「次に来る時刻」を算出
    - [ ] 当日送信済み判定（`lastReminderSentAt` 等）
    - [ ] 頻度（毎日/平日のみ等）がある場合は該当日のみ
  - [ ] アプリ稼働中スケジューリング
    - [ ] 起動時に次回を予約（setTimeout）
    - [ ] 発火後に次回を再予約（自己再スケジュール）
    - [ ] タブ非アクティブ/スリープで遅延する可能性は許容し、起床後の遅延発火でも通知する（仕様化）
  - [ ] バックグラウンド時のフォールバック
    - [ ] 次回起動時に「未測定ならバナー表示」または「即時通知」を出して促す
  - [ ] 将来拡張の足場（Push/Periodic系）
    - [ ] Service Workerに `push` ハンドラの雛形（実運用は別Step）
    - [ ] 利用可否検出の実装（サポートがある場合のみ使う分岐を用意）
- 変更対象（想定ファイル/モジュール）：
  - `js/app.js`（起動時スケジュール、状態管理）
  - `service-worker.js`（pushハンドラ雛形、通知表示の共通化）
  - （追加想定）`js/reminder.js`（時間計算と予約処理を分離する場合）
- 受け入れ基準（完了条件）：
  - 通知ON/許可済みで、アプリを開いたままにしていると設定時刻にリマインダーが表示される。
  - 設定変更（時刻/ON/OFF）で次回予約が更新される。
  - アプリが閉じていた場合でも、次回起動時にリマインドが行われる（フォールバック）。
- テスト/検証：
  - 現在時刻の近い時刻に設定して短時間で動作確認。
  - 日跨ぎ（夜→翌朝）の計算が正しい。
  - 送信済み判定で同一タイミングに二重通知しない。
- 依存関係・注意点：
  - 端末の省電力/バックグラウンド制限でタイマーが遅延・停止する可能性がある。
- リスクと回避策：
  - リスク：ユーザーが「設定時刻なのに通知が来ない」と感じる
  - 回避策：設定画面に「通知は端末設定/省電力の影響を受ける」旨を明記し、確実性が必要なら次フェーズでPush導入する方針を提示

### Plan 5: 総合テスト（許可状態/インストール形態/UX）
- 目的：通知機能が権限状態やPWA形態の違いでも破綻しないことを確認し、リリース可能品質にする。
- 作業内容（チェックリスト）：
  - [ ] 許可状態別のシナリオテスト
    - [ ] 未許可→ONで許可要求→許可する→通知が出る
    - [ ] 未許可→ONで許可要求→拒否する→ONにならない/案内が出る
    - [ ] OS/ブラウザでブロック→案内が出る
  - [ ] PWA形態の差分
    - [ ] 通常タブ
    - [ ] ホーム画面追加（standalone）
  - [ ] リマインダーの境界条件
    - [ ] 朝/夜の直前にONにした場合
    - [ ] 送信済み判定の確認
    - [ ] 日跨ぎの確認
  - [ ] 体験調整
    - [ ] 通知文言（短く・わかりやすい）
    - [ ] アイコン/バッジ（可能なら）
    - [ ] 通知の頻度が過剰にならない
- 変更対象（想定ファイル/モジュール）：
  - `js/app.js`
  - `service-worker.js`
  - `index.html`, `css/style.css`（必要に応じて微調整）
- 受け入れ基準（完了条件）：
  - 主要シナリオで通知/フォールバックが想定通り。
  - クリティカルなエラー（例：通知権限エラーで保存が止まる）がない。
- テスト/検証：
  - 実機（Android）での手動テストを必須。
  - DevToolsのApplication/Service Worker/Notifications周りで状態確認。
- 依存関係・注意点：
  - 実機テストはブラウザとOS設定の影響を強く受けるため、検証手順をメモとして残す。
- リスクと回避策：
  - リスク：端末依存の差が大きい
  - 回避策：サポート対象（Android Chrome + PWAインストール）を明記し、その他はベストエフォートにする

